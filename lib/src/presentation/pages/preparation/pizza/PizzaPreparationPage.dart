import 'package:flutter/material.dart';

class PizzaPreparationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pizza Chef Page'),
      ),
      body: Center(
        child: Text('Esta es la página del Cocinero de Pizzas'),
      ),
    );
  }
}
