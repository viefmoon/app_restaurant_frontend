import 'package:flutter/material.dart';

class KitchenPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cocina'),
      ),
      body: Center(
        child: Text('Esta es la página de la Cocina'),
      ),
    );
  }
}
