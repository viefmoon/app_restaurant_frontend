import 'package:flutter/material.dart';

class HamburgerPreparationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hamburger Chef Page'),
      ),
      body: Center(
        child: Text('Esta es la página del Cocinero de Hamburguesas'),
      ),
    );
  }
}
